# drawing-app-DESKTOP

<h3>Requirements: </h3>

Gradle


<h3>Running Instructions: </h3>
For Windows build and run:

```sh
gradlew build
gradlew run
```

For MacOS/Linux build and run:
```sh
./gradlew build
./gradlew run
```
